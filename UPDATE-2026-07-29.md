# 南开新生问答 · 增量更新说明（2026-07-29）

> 本次更新的全部内容来自 **2026 南开大学新生群（QQ）** 2026-07-24 02:30 ～ 07-29 20:35（北京时间）的聊天记录，
> 由脚本抽取 + 人工核对整理而成，已做匿名化处理。
> **所有内容均为在校学生与新生的群聊转述，不是学校官方口径**，涉及报到、缴费、学籍、军训、分流等事项，
> 请以《新生入学须知》、录取通知书、南开大学官方公众号及各学院正式通知为准。

---

## 一、本次更新总览

| 项目 | 更新前 | 更新后 | 变化 |
|---|---:|---:|---:|
| FAQ 条目 | 51 | 65 | **+14** |
| 回答条目 | 92 | 152 | **+60** |
| 受影响的现有 FAQ | — | — | 28 个（追加 33 条回答） |
| 新增 FAQ 携带的回答 | — | — | 27 条 |

其他结构性变化：

- 每个 FAQ 新增 `freq` 字段（提问热度估算），并以此作为**页面默认排序**依据。
- 28 个现有 FAQ 的关键词做了增补（**只加不删**）。
- 2 个 FAQ 的冲突标记与说明文字做了调整（见第五节）。

**兼容性保证（已逐条校验通过）**：92 条旧回答逐字保留、旧 FAQ 的 ID 与相对顺序不变、旧分类归属零变更、
旧关键词零删除，无重复 ID / 重复问题 / 重复回答。

---

## 二、数据口径与处理流程

1. **导出与切窗**：本轮导出共 17691 条消息（2026-07-23 00:05 → 07-29 20:35）。
   以 **2026-07-24 02:30** 为界剔除与上一轮已处理区间重叠的 3435 条，本轮真正新增区为 **14256 条**。
2. **清洗**：剔除系统消息 184 条、撤回消息 117 条、无文本内容（纯图片/表情/文件）51 条。
3. **去重**：同一发送者、同一文本、且长度 ≤30 字、间隔 <6 小时的重复刷屏合并，去掉 330 条。
   → 最终可用 **13731 条**。
4. **提问热度统计**：在全窗口非系统文本中，用疑问句式正则筛出 **2192 条提问型消息**，
   按主题关键词表归类，得到每个 FAQ 的 `freq`。
5. **整理成文**：所有回答均为**转述改写**，不直接引用原话（个别关键短语除外），
   并标注 `verify`（多人回答一致 / 单一经验回答 / 存在冲突 / 无法确认）与 `applies`（适用条件与核实提示）。
6. **匿名化**：公开数据中来源统一记为「在校学长学姐（QQ群）」「同级新生群友（QQ群）」等标签，
   不出现任何昵称、群名片、QQ 号、微信号、手机号。真实对照关系仅保存在私有审核文件中。

> ⚠ `freq` 的含义：「被问约 N 次」是按关键词自动统计的**热度估算**，
> 样本为 2026-07-23 ～ 07-29 的 QQ 群提问型消息，**仅用于默认排序参考，不是精确计数**。
> 2192 条提问型消息中有 553 条能归入某个主题，其余为闲聊、答复、跑题等未归类内容。
> 有 3 个 FAQ 的 `freq` 为 0（`xiaoqu-when`、`huansushe`、`sushe-menjin`），
> 原因是它们的关键词被更靠前的相近主题（`xiaoqu-how`、`erxuan`、`sushe-nuanqi`）先命中，
> 不代表这些问题没人问；它们在默认排序中排在末尾，用「按分类顺序」即可正常浏览。

---

## 三、新增 14 个 FAQ

| # | ID | 问题 | 分类 | 标记 |
|---:|---|---|---|---|
| 1 | `daiguan` | 大一的"代管学院"是什么？怎么知道自己被分到哪个学院？ | 专业与分流 | — |
| 2 | `tiyuke` | 体育课怎么上？要修几种项目？影响绩点吗？ | 选课与教务 | **存在冲突** |
| 3 | `jidian` | 绩点/成绩怎么算？挂科会怎么样？ | 选课与教务 | — |
| 4 | `xuanxiu-tongshi` | 通识选修课怎么选？大一选课要注意什么？ | 选课与教务 | — |
| 5 | `yuxi` | 大一学什么课？要提前预习高数吗？ | 选课与教务 | — |
| 6 | `siliuji` | 四六级什么时候能考？毕业和保研有要求吗？ | 选课与教务 | — |
| 7 | `baoyan` | 南开保研情况怎么样？大一要为保研做什么？ | 其他 | — |
| 8 | `tijian` | 开学有新生体检吗？要抽血吗？ | 入学手续 | — |
| 9 | `hukou` | 户口要不要迁到学校？不迁会影响医保吗？ | 入学手续 | — |
| 10 | `dangan` | 档案什么时候寄？转团关系的材料怎么弄？ | 入学手续 | — |
| 11 | `jiangxuejin` | 南开有新生奖学金吗？家庭困难有什么资助？ | 费用 | — |
| 12 | `xiaoyiyuan` | 学校有校医院吗？学生医保能报销什么？ | 校园生活 | — |
| 13 | `waimai-shangquan` | 外卖能送到宿舍吗？学校周边有什么吃的？ | 校园生活 | — |
| 14 | `zixingche` | 校内需要买自行车/电动车吗？有共享单车吗？ | 校园环境 | — |

### 要点摘录

- **代管学院**：按大类录取的新生大一被随机分到大类下某个学院代管，代管学院只管理（导员、宿舍、日常事务），
  上课照常、也可选其他学院的课；**分到哪个代管学院基本决定了在哪个校区**。
  通知书上不写，等公众号推文，多位学长说八月中上旬能知道、最晚八月底。
- **体育课**：必修，四个学期，项目在选课系统里抢。
  ⚠ **"一共要修几种项目"群里说法相反**（"四年修四种" vs "不能四年同一种、最多两种初高级"），已标注冲突。
  体育考试成绩计入绩点。
- **绩点**：A 类通识必修课（思政、体育、大学物理、高数、英语，**军训成绩也在内**）计入绩点加权；
  挂科影响转专业，严重可能构成学业警告；科研不算绩点但对评奖保研有帮助。
- **四六级**：大一即可考四级，学长建议早点裸考；毕业本身无四六级要求，部分保研项目有要求。
- **保研**：主要看成绩排名，科研竞赛是加分项。各班型保研率均为个人印象，**务必以学院当年推免文件核实**；
  中外合作办学能否保研群里说法不一，无人给出依据。
- **体检**：**有**新生体检（群里最初有人说"没有"，随后被多位学长更正），一般在军训期间、含抽血、地点大体育馆。
- **户口**：多位学长说"不用迁"；**特别提醒农村户口同学迁出后可能迁不回**；不迁户口一样有学生医保。
- **档案**：由辅导员负责、开学后办，辅导员约八月中旬开始联系新生；
  "转团关系档案袋要不要密封"群里无人能确认，标记为**无法确认**。
- **奖学金**：**没有新生（入学）奖学金**，不按高考成绩直接发奖；困难学生有相应资助。
- **外卖**：只能送到宿舍楼下。八里台西南村是常去商圈；津南西南门外有餐车、附近吾悦广场。
- **自行车**：津南校区大（理科组团→文科食堂约 1 公里），代步工具有用；八里台校内有共享单车。

---

## 四、向 28 个现有 FAQ 追加的 33 条回答

按主题归纳如下（括号内为追加条数）：

**报到与账号**
- `baodao-time`（2）：多方确认 **8 月 29 日报到**；军训"一般报到后第三天开始"，与 8 月 31 日开训吻合。
  报到当天家长可进校、可开车进；报到前可能有"网络注册/线上报到"环节，与到校报到是两回事。
- `jihuo`（2）：7 月下旬官网注册不了，是因为**新生学号还没录入系统**，等学校统一开放。
  账号顺序：学院官方 QQ 群 → 飞书群 → QQ 群作废。
- `feishu`（1）：同上，飞书何时可注册等通知。
- `jiazhangqun`（2）：**重要更正——有微信群自称"官方新生群"，多位学长确认是假的**；
  学校层面没有全校性官方大群，只有各学院本年级的群。辅导员极少联系家长，但挂科等学业问题可能会被联系。

**校区与专业**
- `xiaoqu-how`（1）：大一随机分代管学院 → 基本决定校区；八月中上旬可知。
- `xiaoqu-major`（1）：在校生经验概括——八里台为数、统、理、化、生、文、商＋医（大三）；
  物质科学大类中生化在八里台，环境/材料/药学在津南；物理学代管在八里台、电子光电在津南；工科伯苓在津南。
- `zhuanzhuanye`（1）：二选考上后大一下**仍可再走正常转专业流程**，两者不冲突；
  热门专业二选比转专业更难，"转专业其实没啥限制、报的人也不多"。

**特色班与二次选拔**
- `erxuan`（3）：① 特殊班型能否参加二选，前后出现相反说法，**已标记冲突**；
  ② 伯苓二选考竞赛内容，学长称"二选进的比例很低""工伯报录比基本都 ≤10%"，数学伯苓班不对外招生、名额全部来自二选；
  ③ 二选跨校区搬宿舍在军训之后，有学长现身说法"考进去又退班了"，不报二选的同学开学当天可休息一上午。
- `boling`（1）：工科伯苓动态进出"应该没那么严格"；中途退出回对应学院普通班/大类，不影响后续升学。

**课程与英语**
- `yingyu-fenban`（1）：**关键澄清**——南开没有开学英语分级考试，按高考英语成绩分级（去年约 137 分以上进高级班），
  但分级**不强制**，因此此前"按分分班"与"自己选班"两种说法可以并存；分级不影响四六级考试时间。
- `zaowanzixi`（1）：校园跑每学期 100 公里，可以走完，但必须完成——报名后不跑会导致该门课**绩点上限只有 1.0**。

**军训**
- `junxun-days`（1）：军训"报到后第三天开始"。
- `junxun-zhuang`（1）：鞋要求**纯黑，有白色条纹也不行**；不必买贵的；军训不查床上用品，但开学后可能查寝。
- `mianxun`（1）：军训共 2 学分，属 A 类通识必修、加权进绩点；打靶用电子靶。

**宿舍**
- `sushe-ren`（1）：八里台新生多住 16—19 宿、四人间上下铺，大二搬的宿舍一般是上床下桌；津南基本都是四人间上床下桌。
- `sushe-nuanqi`（1）：宿舍用电有功率限制（群里说约 300 瓦），四台游戏本同开可能跳闸但可申请调整；
  空调租赁一年约三百元，开一天电费约五到六元。
- `sushe-menjin`（1）：除 23:45 锁门外，白天开门约 5:20（**无人复核，仅供参考**），晚归提前跟宿管报备。
- `chuangdian`（1）：报到时校内就有卖床垫等床上用品；津南浴室有浴帘，不用自带。
- `chuangwei`（1）：学院拉群后会发宿舍表格（填生活习惯与室友要求），据此安排。
- `huansushe`（1）：二选跨校区者军训后搬宿舍，不跨校区不用搬。
- `wifi`（1）：网速可以，但厕所等边角覆盖不好，可自接路由器改善。

**生活**
- `shitang`（1）：多位学长认为八里台食堂优于津南；津南分文科/理科食堂，评价文科尚可、理科一般。
- `waimai` 相关见新增 FAQ。
- `kuaidi`（1）：快递可先放驿站，"实测搁那里一个月也不会丢"，仍建议尽快取件。
- `xingli`（1）：行李箱可放柜子外面，不必为塞进柜底特意换箱。
- `shetuan`（1）：百团大战在军训期间；群里出现的有南风动漫社、数学建模社、吉他社、MC 社、击剑、排球等。
- `jinxiao-dengji`（1）：报到当天家长可进校、可开车进，建议按学校要求提前在飞书登记。
- `tuanzuzhi`（1）：档案袋是否密封无人能确认，建议开学问导员。
- `anquan`（1）：**新增防诈提醒**——假"官方新生群"、抖音图文运营招募、代取快递、拼账号等，
  涉及付款/扫码/点链接一律先核实。

---

## 五、标记与说明调整

| FAQ | 调整 | 原因 |
|---|---|---|
| `yingyu-fenban` | `conflict: true → false`，改写 note | 新数据给出了解释：按高考成绩分级但**不强制**，两种旧说法因此可以并存。**两条旧回答原样保留**。 |
| `erxuan` | `conflict: false → true`，补写 note | 新数据出现了与旧回答相反的说法（特殊班型能否参加二选），需提示用户以教务处当年文件为准。 |

---

## 六、关键词增补

28 个现有 FAQ 的 `kw` 数组做了增补，**只加不删**，用于提升搜索命中率。
例如：`baodao-time` 加入「线上报到 / 网络注册 / 第三天 / 家长进校」，
`sushe-nuanqi` 加入「300瓦 / 游戏本 / 跳闸 / 五到六元」，
`anquan` 加入「假官方群 / 抖音运营 / 拼号 / 代取快递」。

---

## 七、前端改动（按提问热度排序）

- `data/faq.json`：每个 FAQ 增加 `freq` 字段，位置在 `cat` 之后，其余结构未动。
- `app.js`：新增 `sortList()`，默认按 `freq` 降序，热度相同者按原始顺序**稳定排列**；
  新增排序按钮事件绑定。
- `index.html`：计数行包进 `.listhead`，加入「🔥 按提问热度 / ☰ 按分类顺序」两个切换按钮。
- `styles.css`：新增排序栏与热度标签样式；问题标题行显示「🔥 被问约 N 次」。
- **未改动**：搜索、锚点、折叠、主题切换、无障碍相关逻辑，以及手工添加的 `.motto` 寄语。
- `dist/index.html` 已按原方式重新生成为单文件离线版（内联 CSS/JS/数据，无外链）。

**测试**：用 Node + 最小 DOM 桩真实执行 `app.js`，验证 65 条全部渲染、热度序列非递增、
切回分类顺序与数据文件完全一致、搜索（空调 / 保研 / 体育课 绩点）、分类筛选、空结果分支均正常。

---

## 八、质量校验与已修正的问题

**溯源**：60 条新增/追加回答**全部**回溯到了原始消息，生成私有审核表
（含真实群名片、UID、消息 ID、原文），共 193 行候选原文。该文件严禁进入公开仓库。

**抽样核对 22 条**，发现并修正 2 个问题：

1. **二选报录比表述错误（内容错误，已修正）**
   原写「平均报录比大概 10%」。回原文核对发现：「50%」那句其实是在说**转专业**，
   「≤10%」才是针对**工科伯苓班二选**的说法。
   → 已改为「二选进的比例很低」「工伯报录比基本都 ≤10%」，并调整 `applies` 为
   「报录比为学长针对工科伯苓班给出的印象值，非官方数据；各班型不同」。

2. **溯源正则误命中图片占位符（工具问题，已修正）**
   `[图片:8FEFFE65...5020BE...]` 中的随机哈希被 `5.?20` 之类的数字正则匹配。
   → 已在溯源脚本中过滤图片/表情/语音/视频/文件占位符后重建审核表。

其余抽样项（户口「没必要 / 毕业还得迁回 / 农村户口迁不回」、四六级「毕业没要求 / 有的保研要求」、
医保「补牙也能报 / 不迁也能有医保·历史遗留政策」、驿站「实测搁那里一个月也不会丢」、
校园跑「100km / 不跑绩点上限 1.0」、军训鞋「纯黑」）均已逐条对照原文确认忠实。

**`.gitignore` 修正**：原文件中 `!data/faq.json          # 唯一允许提交的 JSON` 把注释写在了规则行尾。
`.gitignore` 不支持行尾注释，该行实际被当成一条含中文的无效模式，取反没有生效（当时是靠后面的
`!*/faq.json` 兜住的）。已把注释移到独立行，并补上本轮两个私有文件的显式条目，用
`git check-ignore` 逐条复核通过。

**隐私终检**：从聊天记录中抽出 329 个昵称/群名片、274 个 QQ 号、274 个 UID，对全部公开文件做比对，
QQ 号、UID、手机号、wxid、邮箱、身份证号、9 位以上数字串**零命中**；昵称初筛的少量命中经逐条判读
全部为误撞（普通词与 FAQ ID 片段），已在检查脚本中加白名单注释留档。

---

## 九、使用须知

- 本站所有内容来自**学生间的群聊转述**，不代表南开大学官方立场，也未经学校审核。
- 带「**存在冲突**」标记的条目表示群里出现了相反说法，**必须**以官方文件为准。
- 带「**无法确认**」标记的条目表示群里无人能给出答案，仅作为"这个问题需要问导员"的提示。
- 带「**可能过时**」标记的条目为往年经验，每年安排可能变化。
- 涉及**户籍迁移、档案、缴费、医保**等影响个人长期权益的事项，请务必与家长、原籍相关部门、
  学校对应职能部门核实后再行动，不要仅凭群聊判断。

---

*报告生成时间：2026-07-29 · 数据范围：2026-07-19 ～ 2026-07-29（北京时间）*
