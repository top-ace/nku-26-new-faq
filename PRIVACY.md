# 隐私说明 PRIVACY

本项目由聊天记录整理而来，**隐私优先**是最高原则。

## 已做的处理

1. **回答者一律匿名。** 公开数据（`data/faq.json`）中不含任何真实昵称、QQ 号、QQ UID、手机号、学号等可识别信息。所有来源都替换为匿名标签：
   - `在校学长学姐A/B/…`：根据群名片中的年级前缀（如 23/24/25 级）判断为在校老生，按不同账号分配稳定字母区分。
   - `同级新生群友`：群名片显示为 26 级的新生。
   - `群友`：无法从群名片确认年级/身份的人。
   - 匿名标签**仅依据群成员自己填写的群名片年级前缀**做粗分类，不对任何人的真实身份、专业、住址等做推断。没有把任何人标注为“辅导员”或“管理员”，因为聊天记录中无法确认这些身份。

2. **原始数据不入公开仓库。** 通过 `.gitignore` 排除：
   - `private/`（含 `source-audit.csv` — 真实昵称、原始消息、消息 ID）
   - `review/faq-review.html`（审核页，内嵌真实昵称与原始消息）
   - 原始 QCE 导出 JSON（`*.json`，仅放行 `data/faq.json`）

3. **回答内容做了脱敏与整理。** 删除了口头填充、与答案无关的闲聊；保留数字、日期、地点、否定词、限制条件、适用专业/校区/年级、以及回答者表达的不确定性。凡经过压缩改写的回答，均标注为“整理表述”，不使用虚假的逐字引用。

4. **不含追踪。** 页面无广告、无统计代码、无第三方追踪脚本、无需要隐私授权的功能，搜索完全在浏览器本地运行。

## 提交前请务必自查

在 `git push` 之前，运行一次检查，确认敏感文件没有被提交：

```bash
git status --ignored          # 确认 private/ 和 review/faq-review.html 在 "Ignored files" 里
git ls-files | grep -Ei 'private/|faq-review|group_.*\.json'   # 应无任何输出
```

若上面第二条命令有输出，说明有敏感文件被纳入版本控制，**先移除再提交**：

```bash
git rm --cached <文件路径>
```

## 收到删改请求怎么办

- 如果有群友希望删除或修改与自己相关的某条回答，请直接编辑 `data/faq.json` 删除对应条目并重新部署。
- 匿名标签虽不含真实信息，但如对方仍有顾虑，应尊重其意愿予以移除。

## 免责

本项目为新生自发整理，非南开大学官方发布。内容仅供参考，一切以学校官方最新通知为准。
